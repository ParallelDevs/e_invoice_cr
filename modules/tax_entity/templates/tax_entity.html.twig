{#
/**
 * @file tax_entity.html.twig
 * Default theme implementation to present Tax entity data.
 *
 * This template is used when viewing Tax entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_tax_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('tax_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
